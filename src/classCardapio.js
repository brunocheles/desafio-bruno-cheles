class Cardapio { // Classe para criação de itens para o cardápio
    constructor(codigo, descricao, valor) {
        this.codigo = codigo;
        this.descricao = descricao;
        this.valor = valor;
    }
}

export { Cardapio };